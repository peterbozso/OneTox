<Page
    x:Class="OneTox.View.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneTox.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:OneTox.ViewModel"
    xmlns:friends="using:OneTox.View.UserControls.Friends"
    xmlns:profileSettings="using:OneTox.View.UserControls.ProfileSettings"
    xmlns:messaging="using:OneTox.View.UserControls.Messaging"
    mc:Ignorable="d"
    Loaded="MainPageLoaded"
    d:DataContext="{d:DesignData /SampleData/MainPageViewModelSampleData.xaml}">

    <Page.Resources>
        <ResourceDictionary Source="ResourceDictionaries/MainPageDictionary.xaml" />
    </Page.Resources>

    <Page.DataContext>
        <viewModel:MainViewModel />
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup">
                <VisualState x:Name="ChatState">
                    <VisualState.Setters>
                        <Setter Target="ProfileSettingsBlock.(UIElement.Visibility)" Value="Collapsed" />
                        <Setter Target="AddFriendBlock.(UIElement.Visibility)" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="SettingsState">
                    <VisualState.Setters>
                        <Setter Target="ChatBlock.(UIElement.Visibility)" Value="Collapsed" />
                        <Setter Target="AddFriendBlock.(UIElement.Visibility)" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="AddFriendState">
                    <VisualState.Setters>
                        <Setter Target="ChatBlock.(UIElement.Visibility)" Value="Collapsed" />
                        <Setter Target="ProfileSettingsBlock.(UIElement.Visibility)" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ListView x:Name="FriendList" Grid.Row="0" ItemsSource="{Binding FriendList.Friends}"
                      SelectionChanged="FriendListSelectionChanged"
                      ItemContainerStyle="{StaticResource ListViewItemWithoutPressedVisualStateStyle}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <friends:FriendListItem Margin="8" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <ListView x:Name="ActionIcons" Grid.Row="1" VerticalAlignment="Bottom"
                      Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" Height="45">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem"
                           BasedOn="{StaticResource ListViewItemWithoutPressedVisualStateStyle}">
                        <Setter Property="Padding" Value="0,0,0,0" />
                    </Style>
                </ListView.ItemContainerStyle>

                <TextBlock x:Name="AddFriendIcon" FontFamily="Segoe MDL2 Assets" Text="&#xE710;" FontSize="28"
                           HorizontalAlignment="Center" Width="200" TextAlignment="Center"
                           Tapped="AddFriendIconTapped" Margin="0" Padding="0,9,0,0" Height="45" />
                <TextBlock x:Name="SettingsIcon" FontFamily="Segoe MDL2 Assets" Text="&#xE713;" FontSize="28"
                           HorizontalAlignment="Center" Width="200" TextAlignment="Center" Tapped="SettingsIconTapped"
                           Padding="0,9,0,0" Height="45" />
            </ListView>
        </Grid>

        <messaging:ChatBlock x:Name="ChatBlock" Grid.Column="1" />
        <profileSettings:ProfileSettingsBlock x:Name="ProfileSettingsBlock" Grid.Column="1" Margin="20,0,20,20" />
        <friends:AddFriendBlock x:Name="AddFriendBlock" Grid.Column="1" Margin="20,0,20,20" />
    </Grid>
</Page>