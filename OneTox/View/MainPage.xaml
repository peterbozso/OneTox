<Page
    x:Class="OneTox.View.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneTox.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:userControls="using:OneTox.View.UserControls"
    xmlns:viewModel="using:OneTox.ViewModel"
    mc:Ignorable="d"
    d:DataContext="{d:DesignData /SampleData/MainPageViewModelSampleData.xaml}">

    <Page.DataContext>
        <viewModel:MainPageViewModel />
    </Page.DataContext>

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Orientation="Horizontal">
        <ListView ItemsSource="{Binding FriendList.Friends}" Background="{ThemeResource AppBarBackgroundThemeBrush}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <userControls:FriendListItem Margin="8,12" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Grid />
    </StackPanel>
</Page>