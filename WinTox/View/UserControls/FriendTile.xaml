<UserControl
    x:Class="WinTox.View.UserControls.FriendTile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinTox.View.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="150"
    d:DesignWidth="150">

    <UserControl.Resources>
        <MenuFlyout x:Key="FriendTileFlyout">
            <MenuFlyoutItem Text="delete" Command="{Binding DeleteFriendCommand}" />
        </MenuFlyout>
    </UserControl.Resources>

    <Grid Width="150" Height="150" FlyoutBase.AttachedFlyout="{StaticResource FriendTileFlyout}" RightTapped="Grid_RightTapped">
        <Image Source="/Assets/default-profile-picture.png" Stretch="UniformToFill" />
        <!-- TODO: Replace it with an image with proper resolution and implement binding when user picture loading works! -->
        <Rectangle Height="26" Fill="#7F000000" VerticalAlignment="Bottom" />
        <TextBlock VerticalAlignment="Bottom" Margin="6,0,0,0" FontSize="20" FontWeight="Medium" Text="{Binding Name}" />
    </Grid>
</UserControl>
