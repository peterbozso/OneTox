<UserControl
    x:Class="WinTox.View.UserControls.UserTile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinTox.View.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="70"
    d:DesignWidth="250">

    <Grid Background="{StaticResource BackgroundColor}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- TODO: Implement bindings! -->
        <Image Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Source="/Assets/default-profile-picture.png" Width="70" Height="70" Stretch="UniformToFill" VerticalAlignment="Top"/>
        <Ellipse Width="10" Grid.Row="1" Grid.Column="1" Height="10" Fill="Black" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="5,0,0,5"/>
        <TextBlock Grid.Column="0" Grid.Row="0" FontSize="24" Padding="10,10,10,0" FontWeight="Bold" Foreground="{StaticResource AppBarColor}" HorizontalAlignment="Right"/>
        <TextBlock Grid.Column="0" Grid.Row="1" FontSize="18" Padding="10,0,10,0" Foreground="{StaticResource TextColor}" HorizontalAlignment="Right"/>
    </Grid>
</UserControl>
