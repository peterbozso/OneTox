<UserControl
    x:Class="WinTox.View.UserControls.FriendInfo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinTox.View.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:WinTox.Converters"
    mc:Ignorable="d"
    d:DesignHeight="400"
    d:DesignWidth="300">

    <UserControl.Resources>
        <converters:StatusToColorConverter x:Key="StatusToColorConverter" />
    </UserControl.Resources>

    <StackPanel Background="{StaticResource BackgroundColor}">
        <TextBlock FontSize="42" Margin="0,0,0,10" Foreground="{StaticResource MainColor}" VerticalAlignment="Bottom" Text="{Binding Name}" />
        <TextBlock VerticalAlignment="Bottom" Margin="0,0,0,10" FontSize="22" Foreground="{StaticResource TextColor}" Text="{Binding StatusMessage}" />
        <Grid>
            <Image Source="/Assets/default-profile-picture.png" Width="250" Height="250" Stretch="UniformToFill" HorizontalAlignment="Left" />
            <Ellipse Width="15" Height="15" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="10,0,0,10" Fill="{Binding Path=Status, Converter={StaticResource StatusToColorConverter}}" />
        </Grid>
    </StackPanel>
</UserControl>
